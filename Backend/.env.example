# Database
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/wishlist_ai

# Security
SECRET_KEY=change-me-in-production

# CORS Configuration
# For local development:
CORS_ORIGINS=http://localhost:3000,http://localhost:5173
# For production with Vercel, leave CORS_ORIGINS unset or empty to allow all origins
# Or set specific domains: CORS_ORIGINS=https://your-app.vercel.app,https://your-domain.com

# Environment
APP_ENV=development
DEBUG=false

# JWT Configuration (optional - defaults provided)
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
